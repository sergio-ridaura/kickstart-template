/**
 * Hello world of AssemblyScript,
 * which gives the result of the sum of two numbers.
 */
const defaultHelloWasm = (a: i32, b: i32): i32 => {
  return a + b;
};

export default defaultHelloWasm;
